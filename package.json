{
  "name": "butterfly-museum",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": { "dev": "vite --host 127.0.0.1", "build": "vite build", "preview": "vite preview --host 127.0.0.1", "test": "node --test tests/unit.test.js", "test:e2e": "npm run build && playwright test" },
  "dependencies": { "three": "^0.180.0" },
  "devDependencies": { "vite": "^7.1.5", "@playwright/test": "^1.55.0" }
}
